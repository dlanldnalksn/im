---
import { url } from '../../lib/url';
---

<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="refresh" content={`0;url=${url('intranet/login/')}`} />
    <title>내부망</title>
  </head>
  <body>
    <script define:vars={{ loginUrl: url('intranet/login/') }}>
      window.location.replace(loginUrl);
    </script>
  </body>
</html>
